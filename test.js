



const linedraw = () =>{
    const Canvas = document.querySelector("canvas");
    let $draw = Canvas.getContext('webgl');

    $draw.beginPath();
    $draw.
    $draw.lineTo(100,100)
    $draw.fill()
}

document.addEventListener("mousedown",linedraw);